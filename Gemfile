source "https://rubygems.org"

# Use the Pages stack so local = GitHub build
gem "github-pages", group: :jekyll_plugins

# Ruby 3.4+ stdlib split-outs used by older deps
gem "base64"
gem "csv"
gem "logger"
gem "bigdecimal"

# For local serve on older Jekylls if needed
# gem "webrick"
